<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tydic.filehub.mapper.uoc.SysUserMapper">

    <resultMap id="SysUserMap" type="com.tydic.filehub.dto.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="last_login_time" property="lastLoginTime"/>
    </resultMap>

    <sql id="allColumns">
        id, username, password, nickname, status, create_time, update_time, last_login_time
    </sql>

    <select id="selectByUsername" resultMap="SysUserMap">
        select <include refid="allColumns"/>
        from sys_user
        where username = #{username}
    </select>

    <insert id="insert" parameterType="com.tydic.filehub.dto.SysUser" useGeneratedKeys="true" keyProperty="id">
        insert into sys_user (username, password, nickname, status, create_time, update_time)
        values (#{username}, #{password}, #{nickname}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="updateLastLoginTime">
        update sys_user
        set last_login_time = #{lastLoginTime}
        where username = #{username}
    </update>
</mapper>